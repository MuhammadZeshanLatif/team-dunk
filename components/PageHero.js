import Image from 'next/image';
export default function PageHero({ eyebrow, title, text, image, alt, children, dark=false }) { return <section className={`page-hero ${dark ? 'dark' : ''}`}><div className="container page-hero-grid"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="lead">{text}</p>{children && <div className="button-row">{children}</div>}</div>{image && <div className="hero-picture"><Image src={image} alt={alt} fill priority sizes="(max-width: 800px) 100vw, 50vw" /></div>}</div></section> }
