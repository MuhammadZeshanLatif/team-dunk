import Image from 'next/image'; import Link from 'next/link'; import PageHero from '@/components/PageHero'; import CTA from '@/components/CTA'; import {posts,metadata as makeMeta} from '@/lib/site';
export const metadata=makeMeta('Blog','News, stories and useful ideas from Team Dunk about Ghana, basketball, mentorship, youth development and the Dunkiverse.','/blog');
export default function Blog(){return <><PageHero eyebrow="The Team Dunk journal" title="Ideas that move us forward." text="Stories from the court, the community, Ghana and the creative worlds helping young people learn."/><section className="section"><div className="container blog-grid">{posts.map((p,i)=><article className={i===0?'blog-card featured':'blog-card'} key={p.slug}><Link className="blog-image" href={`/blog/${p.slug}`}><Image src={p.image} alt={p.title} fill sizes={i===0?'100vw':'(max-width: 800px) 100vw, 50vw'}/></Link><div><span>{p.category} · {p.date}</span><h2><Link href={`/blog/${p.slug}`}>{p.title}</Link></h2><p>{p.excerpt}</p><Link className="text-link" href={`/blog/${p.slug}`}>Read full story →</Link></div></article>)}</div></section><CTA/></>}
